export default {
  Func_Runs: {
    rules: [{ required: true, message: 'This field is required.' }],
  },
  durations: {
    rules: [{ required: true, message: 'This field is required.' }],
  },
  regressor_names: {
    rules: [{ required: true, message: 'This field is required.' }],
  },
  regressors: {
    rules: [{ required: true, message: 'This field is required.' }],
  },
}
